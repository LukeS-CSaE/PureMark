> ⚠️ **文档状态（2026-08-10）**：本文件是 PureMark 的**历史需求基线**（v1 对齐规格），记录"最初如何规划"。其中多数功能已在真实代码中落地，但产品名已定 **PureMark**、编辑器内核已改为 CodeMirror 6、样式/交互有多处演进。**当前唯一权威认知总览 = `docs/项目认知与现状总览.md`**（含代码核对、功能落地表、遗留债清单），凡冲突以它为准。

# pureMD 极简 Markdown 编辑器 · 对齐规格（v1 需求与样式对齐）

> 用途：开发前确认清单。本文档**只对齐功能与样式**，不含代码与架构设计；打勾项需用户拍板。
> 设计依据：公区 `设计稿.html`（pureMD 编辑器，现代扁平风）。
> 技术栈：pnpm + Tauri（Rust 后端 + 前端 WebView），Windows 桌面 .exe。

***

## 1. 产品定位与一句话概述

- **定位**：极简、本地优先（local-first）的 Markdown 编辑器桌面应用。无账号、无云同步、零干扰，专注写作本身。
- **一句话概述**：`- [ ]` **pureMD 是一款基于 Tauri 的极简本地 Markdown 编辑器——开箱即用、纯本地文件、所见即所得的写作工具。**

***

## 2. 核心功能清单（P0 / P1 / P2）

> P0 = v1 必须；P1 = 后续迭代；P2 = 可选/远期。默认勾选项为本规格建议的 v1 范围。

### P0 — v1 必须

- [x] **窗口外壳（App Shell）**：自定义标题栏，左侧 logo + 产品名，右侧 最小化 / 窗口 / 关闭 三个窗口控制按钮（Tauri titlebar 接管）。
- [x] **打开文件夹 + 文件树（EXPLORER）**：浏览本地文件夹，列出 `.md` 文件，点击在标签页打开；支持隐藏 / 显示侧边栏。
- [x] **多标签页编辑**：支持新建、打开、关闭标签；同一工作区多文件切换。
- [x] **Markdown 编辑**：纯文本编辑区，内容驻留内存。
- [x] **三种视图（分段控件）**：`编辑`（纯源码）/ `分屏`（左源码右预览）/ `预览`（纯渲染）。默认视图见开放问题 #7。
- [ ] **基础格式化工具栏**：标题 / 粗体 / 斜体 / 删除线 / 行内代码 / 无序列表 / 有序列表 / 任务列表 / 引用 / 链接 / 图片 / 表格。
- [x] **实时预览渲染**：支持 h1–h4、段落、列表、引用、kbd 胶囊、代码卡片（code card）等基础样式。
- [x] **保存（Ctrl+S）**：写回原文件；未保存态有脏标记。
- [x] **状态栏统计**：光标 Ln/Col、行数、词数、字数；右侧显示编码（UTF-8）、语言（Markdown）。
- [x] **未保存保护**：关闭标签 / 退出应用前，若有未保存修改弹确认框（Tauri dialog）。

### P1 — 后续迭代

- [x] **编辑器内搜索 / 替换**：工具栏已预留「搜索」按钮，v1 可先做查找、替换后置。
- [x] **设置面板**：基础偏好（字体、字号、默认视图等）；工具栏已预留「设置」按钮。
- [ ] **侧边栏折叠记忆**：关闭后下次启动保持隐藏。
- [x] **代码块「复制」按钮**：预览态代码卡片右上角一键复制。

### P2 — 可选 / 远期

- [ ] **导出 HTML / PDF**（见开放问题 #3）。
- [x] **主题切换**（见开放问题 #4）。
- [ ] **演讲 / 专注模式**（设计稿文件列表含「演讲模式.md」，疑似历史意图，待确认）。
- [x] **自动保存 / 本地草稿**：防丢失，存于本地缓存。

***

## 3. 界面与样式规范（提炼自设计稿）

### 3.1 设计 Token（CSS 变量，建议集中定义一份 `theme.css`）

| 分类   | Token                                                         | 值                                                                                                                        |
| ---- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 主色   | `--primary` / `--primary-hover` / `--primary-foreground`      | `#0ea5e9` / `#0284c7` / `#ffffff`                                                                                        |
| 主色柔底 | `--primary-soft`                                              | `rgba(14,165,233,0.10)`                                                                                                  |
| 中性面  | `--background` / `--surface` / `--surface-2` / `--surface-3`  | `#f4f6f8` / `#ffffff` / `#eef0f2` / `#e4e7eb`                                                                            |
| 文本   | `--foreground` / `--foreground-muted` / `--foreground-subtle` | `#1f2328` / `#656d76` / `#8c959f`                                                                                        |
| 边框   | `--border` / `--border-subtle` / `--input` / `--ring`         | `#d9dde1` / `#e9ebef` / `#d9dde1` / `rgba(14,165,233,0.35)`                                                              |
| 圆角   | `--radius-sm` / `--radius-md` / `--radius-lg`                 | `4px` / `8px` / `12px`                                                                                                   |
| 阴影   | `--shadow-1` / `--shadow-2` / `--shadow-3`                    | 极淡 / 卡片浮起 / 大浮层（见设计稿）                                                                                                    |
| 字体   | `--font-sans` / `--font-mono`                                 | `Inter,"Noto Sans SC","PingFang SC","Microsoft YaHei",system-ui` / `JetBrains Mono,Fira Code,SF Mono,Consolas,monospace` |
| 尺寸   | 侧边栏宽 / 顶栏高 / 状态栏高 / 工具栏高                                      | `248px` / `52px` / `34px` / `46px`                                                                                       |

> 阴影具体值（直接取自设计稿）：
>
> - sh1：`0 1px 2px rgba(31,35,40,.04), 0 1px 1px rgba(31,35,40,.02)`
> - sh2：`0 8px 24px -8px rgba(31,35,40,.12)`
> - sh3：`0 24px 60px -20px rgba(31,35,40,.22)`

### 3.2 布局结构（自上而下 / 自左而右）

```
┌─ 顶栏 Header 52px：左 [logo+pureMD]  右 [－ □ ✕] ───────────────┐
├─┬────────── 侧边栏 248px ────────┬─ 工作区 ────────────────────┤
│ │ EXPLORER                [‹]    │  标签页 tab ×tab ×tab …      │
│ │ [ Open Folder ]                │ ┌─ 编辑器卡片 (白底/圆角/sh2)┐│
│ │ ▸ 欢迎.md                      │ │ 工具栏 46px：新建/打开/保存 ││
│ │ ▸ Markdown 语法.md (active)    │ │ | 标题/粗/斜/删/代码        ││
│ │ ▸ 实时编辑.md                  │ │ | 列表/任务/引用/链接/图/表 ││
│ │ ▸ 演讲模式.md                  │ │ | [编辑|分屏|预览]  搜索 设置││
│ │                               │ │ ───────────────────────── ││
│ │                               │ │ 内容区：实时渲染预览        ││
│ │                               │ └────────────────────────────┘│
├─┴───────────────────────────────┴─────────────────────────────┤
└─ 状态栏 34px：Ln 1,Col 1 · 89 lines · 297 words · 1,248 字  | UTF-8 · Markdown ─┘
```

### 3.3 关键组件视觉

- **顶栏 Logo**：30×30 主色圆角方块（radius-md），内嵌 `pen-tool` 图标，白色；右侧窗口三键为线性图标 `minus` / `square` / `x`，hover 变深。
- **侧边栏 EXPLORER 头**：标题 11px / 700 / 字距 0.06em / subtle 灰；右侧 `panel-left-close` 折叠按钮。
- **Open Folder 按钮**：全宽、描边、13px 文字 + `folder-open` 图标，hover 变 surface-2。
- **File item**：左 `file-text` 图标 + 13px 文件名；hover → surface-2 底 + foreground 字；active → primary-soft 底 + primary 字。
- **Tab 标签**：13px 文件名 + `x` 关闭；active → 白底 + primary 字 + 细边框 + sh1 浮起。
- **工具栏按钮**：30×30 圆角图标按钮（radius-md），muted 字、hover surface-2；active → primary-soft 底 + primary 字。工具组间用 1px 分隔线。
- **分段控件（编辑/分屏/预览）**：连体三段，active 段 = primary 底 + 白字，其余描边 muted。
- **编辑器内容区**：padding `32px 40px`；h1 28/700、h2 22/600 带下边框、h3 18/600；p 行高 1.7 色 muted；`kbd` 小胶囊（surface-2 底 + 描边 + mono）；`code card` 浅底卡片 + 右上「复制」按钮。
- **状态栏**：12px、muted 字；条目间用半透明小圆点分隔。

### 3.4 字体与图标

- **字体**：正文 sans（Inter + 中文回退），代码 / kbd 用 mono（JetBrains Mono / Fira Code / Consolas）。
- **图标**：lucide 线性图标集，细描边、低饱和；窗口控制、文件、工具栏统一用 lucide。

### 3.5 落地到 Tauri 前端（方式建议，非架构）

- 所有 token 写入一份共享 CSS 变量文件；前端框架通过 Tailwind `@theme inline` 映射成工具类（`bg-background` / `text-primary` 等），与设计稿一致。
- 窗口外壳交给 Tauri 自定义标题栏（`decorations: false`），顶栏三键调用 Tauri `window` API（minimize / toggleMaximize / close）。
- 布局尺寸统一引用 token 变量（如 `var(--puremd-sidebar-width)`），不硬编码数字。
- 整体默认 **Light 主题**；深色模式见 P2 / 开放问题 #4。

***

## 4. 交互与边界（含建议，待确认项已标注）

- **本地优先**：所有文件读写仅走本地磁盘，无账号、无云端、无 telemetry。
- **未保存保护**：标签 / 文件 / 窗口关闭前，若存在脏标记（dirty）则弹 Tauri 确认框。
- **存储模型**：v1 直接读写原文件（保存即覆盖）；是否需自动保存草稿 → `- [ ]` 建议放 P2（见开放问题 #8）。
- **导出**：是否支持导出 HTML / PDF → `- [ ]` 建议放 P2，v1 不做（见开放问题 #3）。
- **主题**：v1 仅 Light；深色模式 → `- [ ]` 建议放 P2（见开放问题 #4）。
- **视图默认与记忆**：分屏是否为默认视图 → `- [ ]` 见开放问题 #7；是否记忆上次视图（存本地配置）→ `- [ ]` 建议「记忆」。
- **文件类型**：v1 是否仅处理 `.md` / `.markdown` → `- [ ]` 见开放问题 #2。

***

## 5. 待用户确认的开放问题（请逐项拍板）

- [ ] **Q1 产品名**：是否就叫 **pureMD**？或微调（如 pureMark / MD pure）？
  - [ ] 叫PureMark
- [ ] **Q2 文件范围**：v1 只需「打开文件夹」浏览本地 `.md` 文件树？还是也要支持「打开单个文件」？是否限定只 `.md`？
  - [ ] 文件夹和单个都要支持，只限定md
- [ ] **Q3 导出**：v1 是否需要导出 HTML / PDF？还是全部后置到 P2？
  - [ ] 先不做导出
- [ ] **Q4 深色模式**：v1 是否需要深色模式？还是先只 Light、P2 再加？
  - [ ] 深色模式通过主题实现
- [ ] **Q5 窗口初始尺寸**：建议初始 `1100×720`、最小 `860×560`、是否默认最大化启动？
  - [ ] 1500\*830
- [ ] **Q6 搜索 / 设置**：工具栏已预留按钮——v1 是否实现（至少查找）？还是先占位？
  - [ ] 实现查找
- [ ] **Q7 默认视图**：默认 `分屏` 还是 `编辑`？（设计稿默认 active = 分屏）
  - [ ] 默认为编辑
- [ ] **Q8 自动保存**：是否需要本地草稿 / 自动保存防丢失？还是仅手动 Ctrl+S？
  - [ ] 需要
- [ ] **Q9 渲染库偏好**：markdown-it（插件生态好）或 marked（轻量）？无偏好则交架构师定。
  - [ ] 轻量，要求运行占用内存极小
- [ ] **Q10 演讲 / 专注模式**：设计稿含「演讲模式.md」，是否纳入规划？还是砍掉？
  - [ ] 暂不计划

***

## 6. 建议的技术落地提示（点到为止，详细架构由架构师负责）

- **壳**：pnpm + Tauri 2.x（Rust）桌面壳，原生窗口 + WebView；`tauri build` 产 Windows `.exe`（可选签名）。
- **前端框架**（任选，建议沿用 Tailwind 体系以对齐设计稿）：React / Vue / Svelte + Tailwind CSS v4（`@theme inline` 承接 token）。
- **状态 / 多标签**：轻量方案即可（如 Zustand / Pinia / Svelte stores）。
- **Markdown 渲染**：markdown-it 或 marked；代码高亮可选 highlight.js / Shiki。
- **文件读写**：Tauri `fs` + `dialog`（`open` 文件夹 / `save`）；窗口控制走 `window` API。
- **本地配置**：视图模式、字体偏好等用 Tauri store / 本地文件持久化。
- **图标**：lucide（设计稿用 `lucide@1.8.0`，建议升级到稳定版 `@lucide/*`）。

